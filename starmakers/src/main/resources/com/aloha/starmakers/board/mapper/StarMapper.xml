<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.aloha.starmakers.board.mapper.StarMapper">

    <!-- 게시판 조회 -->
    <!-- <select id="select" resultType="starCard">
        SELECT *
        FROM star_board
        WHERE status = "홍보중"
    </select> -->

    <!-- 글 등록 -->
    <insert id="insert">
        INSERT INTO star_board(title, writer, content, user_no, pay_no, status, category1, category2, type, start_date, end_date)
        VALUES (#{title}, #{writer}, #{content},#{userNo},#{payNo},#{status},#{category1},#{category2},#{type},#{startDate},#{endDate})
    </insert>

    <!-- 결제 정보 등록 -->
    <!-- <insert id="payment">

    </insert> -->

</mapper>